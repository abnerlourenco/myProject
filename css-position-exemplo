<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Css Position Exemplo</title>
    <style>
        * {
            box-sizing: border-box;
        }
        .container {
            margin-top: 8rem;
            width: 90vw;
            height: 150vh;
            padding: 1rem;
            /* position: relative; */

            display: flex;

            border: 1px solid black;
        }
        .item {
            width: 6rem;
            height: 6rem;

            border: 1px solid black;

            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* O static já padrão da pagina, portanto nao haverá nenhuma modificação.
        Nesse exemplo, mesmo colocando para mover para a direita ele permanece na 
        posição original */
        .static {
            position: static;
            right: 24px;
            background: #f447;
        }

        /* O position relative é uma posição relativa a sua posição original, 
        nesse caso ele coloca uma margem esquerda e ainda um alinhamento a 
        esquerda do posicionamento original; */
        .relative {
            position: relative;
            left: 6rem;
            bottom: 6rem;
            margin-left: 6rem;
            background: #4559;
        }

        /* O position absolute faz com que o elemento se sobreponha a todos elementos 
        e sua posição é descrita baseada no primeiro elemento pai que tiver qualquer 
        posicionamento diferente de static(padrão). Caso não haja nenhum Elemento 
        pai nessas condições, ele toma como referencia o elemento body. */

        /* Para verificar o elemento ficar referenciado pelo container */
        .absolute {
            position: absolute;
            top: 0;
            left: 0;

            background: green;
        }
        .fixed {
            position: fixed;
            top: 0;
            left: 15rem;
            background: #298;

            width: 20rem;
        }
        .sticky {
            position: sticky;

            top: 10rem;

            background: red;
            height: 30rem;
        }

    </style>
</head>
<body>
    <div class="container">
        <div class="item static">STATIC</div>
        <div class="item relative">RELATIVE</div>
        <div class="item absolute">ABSOLUTE</div>
        <div class="item fixed">FIXED</div>
        <div class="item sticky">STICKY</div>
    </div>
</body>
</html>
